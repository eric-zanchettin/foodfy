{% extends "layout.njk" %}

{% block head %}
    <title>Foodfy - Home</title>
{% endblock %}

{% block content %}

        <div id="rectangle">
            <h1>As melhores receitas</h1>
            <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
            <img id="chef" src="https://i.imgur.com/ax56hOK.png" />
        </div>

        <div id="top">
            <h1>Mais acessadas</h1>
            <section id="grid-top">
                {% for item in items %}
                <div class="cards" id="{{item.id}}">
                    <a>
                        <img src="{{item.image}}" alt="{{item.alt}}" />
                        <h1>{{item.title}}</h1>
                        <p>por {{item.author}}</p>
                    </a>
                </div>
                {% endfor %}
            </section>
        </div>
    <script src="scripts.js"></script>
    
{% endblock %}